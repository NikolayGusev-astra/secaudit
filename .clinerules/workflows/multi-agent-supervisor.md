Workflow: SmartFinance Multi-Agent Supervisor
🛑 ОБЯЗАТЕЛЬНЫЙ ПРОТОКОЛ (Из SYSTEM_INSTRUCTION.md)
Перед началом работы ты ОБЯЗАН:

Выполнить в терминале: cat PROJECT_STATE.md
Сверить свои планы с разделом Технологический Стек и Бухгалтерские Правила.
Если планируешь использовать Python -> СТОП. Стек говорит Node.js.
Если планируешь использовать FLOAT -> СТОП. Стек говорит NUMERIC.
Шаг 1: Синхронизация и Контекст
Прочитай SYSTEM_INSTRUCTION.md полностью.
Прочитай docs/IMPLEMENTATION_PLAN.md (найди Текущую фазу).
Прочитай docs/ACCOUNTING_CONSTITUTION.md (Золотое правило, Кредитные карты).
Обнови контекст: Мы на [Имя Фазы] из IMPLEMENTATION_PLAN.md.
Шаг 2: Фаза Архитектора (Architect Role)
Инструкция: Переключись на роль Architect (.role architect).

Задача:

На основе пользовательской задачи и docs/IMPLEMENTATION_PLAN.md создай docs/TASK_SPEC.md.
В TASK_SPEC.md ОБЯЗАТЕЛЬНО укажи:
Constraints (Ограничения): Node.js only (NO Python), NUMERIC only (NO Float).
Accounting Rules: Ссылка на docs/ACCOUNTING_CONSTITUTION.md.
Tech Stack: Next.js, Supabase, Exceljs.
Шаг 3: Фаза Исполнителя (Backend-Executor Role)
Инструкция: Переключись на роль Backend-Executor (.role backend-executor).

Задача:

Прочитай docs/TASK_SPEC.md.
Реализуй задачу.
ПРОВЕРКА:
Все типы денег в SQL -> NUMERIC(15,2).
Покупка в кредит -> Expense. Погашение -> Transfer.
Используй exceljs для парсинга.
Правило безопасности (Rabbit Hole):Если ошибка повторяется > 2 раз:

ПРЕКРАТИ попытки.
Открой PROJECT_STATE.md.
Запиши ошибку в раздел ⚠️ Известные проблемы / Блокеры.
Сообщи: "Я зафиксировал проблему в PROJECT_STATE.md. Требуется помощь человека."
Шаг 4: Фаза Валидатора (Validator Role)
Инструкция: Переключись на роль Validator (.role validator).

Задача:Проверить результат работы Исполнителя.

Чек-лист отказа (❌ FAIL):

В коде есть import python / import pandas? -> FAIL.
В SQL есть FLOAT / DOUBLE? -> FAIL.
В RLS Policy используется NEW. или OLD.? -> FAIL (Критическая ошибка из SYSTEM_INSTRUCTION.md).
Погашение кредита записано как Expense? -> FAIL (Двойной счет).
Если FAIL:Верни Исполнителя на Шаг 3. Формат: ⛔ VALIDATION FAILED: [Причина]. Переделай используя [Правильный стек].

Если PASS:

Обнови PROJECT_STATE.md (Журнал последних действий).
Обнови Текущую задачу на следующую.
Заверши работу: attempt_completion.